---
name: exec
description: Run Kiro CLI non-interactively
---

# exec

Use `kch exec` for a single non-interactive Kiro prompt.

```bash
kch exec --agent kiro_default "Summarize the current directory"
```
