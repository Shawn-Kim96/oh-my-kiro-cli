---
name: plan
description: Generate a staffing and verification plan for kch work
---

# plan

Use `kch plan` to map a task to roles and a launch command.

```bash
kch plan "Mature worker protocol tests" --workers 3 --mode team
```
