---
name: uninstall
description: Remove kch runtime state with explicit apply
---

# uninstall

Use `kch uninstall` to preview runtime state removal. Add `--apply` to remove selected runtime directories, or `--purge --apply` to remove the state root.
