---
name: notepad
description: Read and write kch session notepad memory
---

# notepad

Use `kch notepad` for compact persistent notes.

```bash
kch notepad write working "Dispatch retry logic verified"
kch notepad read --json
```
