---
name: sparkshell
description: Run a bounded shell command or capture a tmux pane
---

# sparkshell

Use `kch sparkshell` for simple command execution or pane capture.

```bash
kch sparkshell pwd
kch sparkshell --tmux-pane %1 --tail-lines 120
```
